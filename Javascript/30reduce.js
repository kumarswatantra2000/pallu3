const numMy = [1,2,3,4,5]

//const swatantrauser = pallu.reduce(function (ace, currval){
   // return ace + currval

//}, 0)

const myTotal = numMy.reduce( (acc, curr) => acc+curr, 0)

console.log("myTotal");
